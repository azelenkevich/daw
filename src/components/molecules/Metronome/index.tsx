import styles from './styles.module.scss';

const Metronome = () => {
  return (
    <div className={styles['metronome']}>
      <button onClick={() => {}}>metronome</button>
    </div>
  )
}

export default Metronome